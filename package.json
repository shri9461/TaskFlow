{
  "name": "taskflow-workspace",
  "version": "1.0.0",
  "description": "TaskFlow root workspace runner",
  "scripts": {
    "dev": "npm run dev --prefix TaskFlow-main",
    "backend": "npm run dev --prefix TaskFlow-main/backend",
    "frontend": "npm run dev --prefix TaskFlow-main/frontend"
  }
}
